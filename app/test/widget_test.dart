// Domyślny szablon `flutter create` referował `MyApp` które już nie istnieje.
// Testy widgetów dodawane stopniowo w `test/widget/`.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
